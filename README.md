# PBL

# Attention
最初にbashもしくはコマンドプロンプト上で(VSCodeでもいけるけど)
```
git pull
```
で最新状態にしておく

# ブランチを変えるとき
```
git switch sub
```

# ファイルを上げるとき
0. code . でVSCodeを開く
1. ファイルを(例：txtフォルダの中に)ドラッグアンドドロップ
2. 左のバーの上から3つ目(ソース管理)を押す
3. コミットボタンを押す
4. 何かメッセージを書いて、上のタブの×を押す
5. 保存しますか？に　はい
6. 変更を同期ボタンを押す